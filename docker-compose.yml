services:
  jello:
    image: jellothompson.com:v0.1.0
    restart: unless-stopped
    ports:
      - "8081:8080"
    networks:
      - rproxy
  winecraft:
    image: winecraft.dev:v0.1.3
    restart: unless-stopped
    ports:
      - "8082:8080"
    networks:
      - rproxy
  connect4:
    image: connect4.winecraft.dev:v0.1.0
    restart: unless-stopped
    ports:
      - "8083:8080"
    networks:
      - rproxy
  nginx: 
    image: nginx:latest
    restart: unless-stopped
    ports:
      - "443:443"
      - "80:80"
    volumes:
      - ./nginx-conf:/etc/nginx/conf.d
      - ./certbot/www/:/var/www/certbot/:ro
      - ./certbot/conf/:/etc/letsencrypt/:ro
    networks:
      - rproxy
  certbot:
    image: certbot/certbot:latest
    restart: no
    volumes:
      - ./certbot/www/:/var/www/certbot/:rw
      - ./certbot/conf/:/etc/letsencrypt/:rw
networks:
  rproxy:
    name: rproxy

